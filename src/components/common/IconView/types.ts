import type { ButtonProps, IconProps, TooltipInstance } from "element-plus";

export type IconConfig = Partial<IconProps>;
export type ButtonConfig = Partial<ButtonProps>;
export type ToolTipConfig = Partial<TooltipInstance["$props"]>;
